#! /bin/sh

export NETMONDIR="$HOME/.local/networkmonitor"

if [ ! -d $NETMONDIR ]; then
        mkdir -p $NETMONDIR
fi

export HOST_LIST=$NETMONDIR/host-list
export NMAP_SCAN=$NETMONDIR/nmap-scan
export LAST_FIND=$NETMONDIR/last-find
export LAST_SCAN=$NETMONDIR/last-scan
