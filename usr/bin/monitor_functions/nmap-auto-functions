#! /bin/sh

NETMONDIR="$HOME/.local/networkmonitor/"

if [ -d $NETMONDIR/ ]; then
        HOST_LIST=$NETMONDIR/host-list
        NMAP_SCAN=$NETMONDIR/nmap-scan
else
        mkdir -p $NETMONDIR
        HOST_LIST=$NETMONDIR/host-list
        NMAP_SCAN=$NETMONDIR/nmap-scan
fi
